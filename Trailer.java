public class Trailer extends Room {
   //since Trailer is a different shape could extend to add unique Area properties locally
   public Trailer(String name, Board board){
      super(name, board);
   }
}